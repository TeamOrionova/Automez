/**
 * Structured Data Component
 * Loads external JSON-LD schema markup for AI agents and search engines
 */

export function StructuredData() {
    return (
        <script src="/structured-data.js" defer />
    );
}
